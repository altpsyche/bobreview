# BobReview Dependencies
# ========================
# This is the SINGLE SOURCE OF TRUTH for all runtime dependencies.
# setup.py and pyproject.toml read from this file.
#
# To add a dependency: Add it here, then run: pip install -e .
# To update: Change version here, then run: pip install --upgrade -e .

# Required Dependencies
# ---------------------
openai>=1.0.0,<2.0.0     # OpenAI API client for LLM-powered analysis
scipy>=1.11.0,<2.0.0     # Statistical functions for accurate t-distribution
bleach>=6.0.0,<7.0.0     # HTML sanitization for LLM-generated content
jinja2>=3.1.0            # Template engine for CMS-style reports

# Optional Dependencies (Recommended)
# -----------------------------------
tqdm>=4.65.0,<5.0.0      # Progress bars for better UX
colorama>=0.4.6,<1.0.0   # Colored terminal output

# LLM Provider Dependencies (install based on your needs)
# --------------------------------------------------------
# anthropic>=0.18.0     # Anthropic Claude API (install: pip install anthropic)
# httpx>=0.27.0         # HTTP client for Ollama (install: pip install httpx)

# Future Dependencies (Planned - uncomment when needed)
# ------------------------------------------------------
# pandas>=2.0.0          # CSV/data manipulation (Phase 2)
# PyYAML>=6.0            # Config file support (Phase 3)
# weasyprint>=60.0       # PDF export (Phase 4)
# pytest>=7.4.0          # Testing framework (Phase 6)
